        std::cout << "a: ";
        for (std::vector<int>::iterator it = a.begin(); it < a.end(); ++it){
            std::cout << *it << " ";
        }
        std::cout << std::endl << "b: ";
        for (std::vector<int>::iterator it = b.begin(); it < b.end(); ++it){
            std::cout << *it << " ";
        }
        std::cout << std::endl;
    std::cout << "jacob" << std::endl;
    for (std::vector<size_t>::iterator itj = _Jacob.begin(); itj < _Jacob.end(); itj++){
        std::cout << *itj << " ";
    }
    std::cout << std::endl;
